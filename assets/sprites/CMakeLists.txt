
file(GLOB HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
file(GLOB OBJECTS "${CMAKE_CURRENT_SOURCE_DIR}/*.s")

install(FILES ${OBJECTS} DESTINATION lib)
install(FILES ${HEADERS} DESTINATION include)

set_directory_properties(PROPERTIES ADDITIONAL_MAKE_CLEAN_FILES "${OBJECTS};${HEADERS}")